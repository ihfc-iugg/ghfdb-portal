"""
Contract tests for API schema validation.

Contract tests validate that public API endpoints maintain backward
compatibility by checking response schemas, field names, data types,
and error formats against documented contracts.

Run with: pytest -m contract
"""
