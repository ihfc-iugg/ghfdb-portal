"""Tests for the published-column mapping (``project/ghfdb/columns.py``).

Populated by US-3 (T063-T077). Empty for now: Phase 1 only scaffolds the
test tree that mirrors ``project/ghfdb/`` (T001), and ``columns.py`` itself
does not exist yet.
"""

import pytest

pytestmark = pytest.mark.ghfdb
