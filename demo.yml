version: '3'
name: ghfdb

volumes:
  postgres: {}
  postgres_backups: {}
  media: {}

services:

  django: &django
    build:
      context: .
      dockerfile: ./compose/production/django
    image: production_django
    depends_on:
      - postgres
      - redis
      - minio
    command: start-django
    ports:
      - "5000:5000"
    env_file:
      - stack.env
    labels:
      - traefik.enable=true
      - traefik.http.routers.django.tls=true
      - traefik.http.routers.django.rule=Host(`${DJANGO_HOST_NAME}`)
      - traefik.http.routers.django.entrypoints=websecure
      - traefik.http.services.django.loadbalancer.server.port=5000
      - traefik.http.middlewares.csrf.headers.hostsProxyHeaders=X-CSRFToken
  postgres:
    image: geoluminate/postgis:latest
    env_file:
      - stack.env
    volumes:
      - postgres:/var/lib/postgresql/data
      - postgres_backups:/backups
  redis:
    image: redis:6
  celeryworker:
    <<: *django
    command: start-celeryworker
    ports: []
    labels: []
  celerybeat:
    <<: *django
    command: start-celerybeat
    ports: []
    labels: []
  flower:
    <<: *django
    command: start-flower
    ports:
      - "5555:5555"
    labels:
      - "traefik.enable=true"
      - traefik.http.routers.flower.tls=true
      - "traefik.http.routers.flower.rule=Host(`tasks.${DJANGO_HOST_NAME}`)"
      - "traefik.http.routers.flower.entrypoints=websecure"
      - traefik.http.services.flower.loadbalancer.server.port=5555
  minio:
    image: quay.io/minio/minio:latest
    volumes:
      - media:/minio
    env_file:
      - stack.env
    ports:
      - "9001:9000"
      - "9002:9001"
    command: server /minio
    labels:
      - traefik.enable=true
      - traefik.http.routers.media.tls=true
      - traefik.http.routers.media.entrypoints=websecure
      - traefik.http.routers.media.rule=Host(`media.${DJANGO_HOST_NAME}`)
      - traefik.http.routers.media.service=media
      - traefik.http.services.media.loadbalancer.server.port=9001

      - traefik.http.routers.minio.tls=true
      - traefik.http.routers.minio.entrypoints=websecure
      - traefik.http.routers.minio.rule=Host(`media.${DJANGO_HOST_NAME}`) && PathPrefix(`/dashboard`)
      - traefik.http.routers.minio.service=minio
      - traefik.http.services.minio.loadbalancer.server.port=9002
  traefik:
    image: "traefik:latest"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    ports:
      - "80:80"
      - "8080:8080"
    command:
      - --api.insecure=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
      - --entrypoints.web.http.redirections.entryPoint.to=websecure
      - --entrypoints.websecure.address=:443
      # - traefik.http.routers.portainer.tls=true
      # - traefik.http.routers.flower.rule=Host(`management.${DJANGO_HOST_NAME}`)"
      # - traefik.http.routers.flower.entrypoints=websecure"
      # - traefik.http.services.flower.loadbalancer.server.port=9000
