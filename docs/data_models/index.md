# Data Models

How the Global Heat Flow Database is structured, from the conceptual entities down to the
tables and relationships the portal stores.

Start with the conceptual model for the entities and the reasoning behind the parent-child
split. The entity relationship diagram covers the same ground at the level of tables, keys
and cardinalities. Reading the published structure covers the other direction: how the stored
graph is presented in the flat shape the commission publishes.

```{toctree}
:maxdepth: 2

ghfdb-conceptual-model
ghfdb-erd
published-structure
```

Field-level definitions for the published database live in
[GHFDB Fields](../ghfdb_fields.md).
